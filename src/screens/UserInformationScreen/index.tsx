import React from 'react';
import {View} from 'react-native';
import {Text} from 'react-native-paper';
import {Layout} from '../../components/Layout';

export const UserInformationScreen = () => {
  return (
    <Layout>
      <View>
        <Text>UserInformation Screen</Text>
      </View>
    </Layout>
  );
};
